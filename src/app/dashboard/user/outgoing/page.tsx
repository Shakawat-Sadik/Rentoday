import OutgoingContent from './outgoing-content'

// Dashboard → Outgoing Requests (booking requests the user has sent).
export default function OutgoingRequestsPage() {
  return <OutgoingContent />
}
