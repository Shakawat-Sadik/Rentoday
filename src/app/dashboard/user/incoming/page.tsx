import IncomingContent from './incoming-content'

// Dashboard → Incoming Requests (booking requests on the user's own listings).
export default function IncomingRequestsPage() {
  return <IncomingContent />
}
