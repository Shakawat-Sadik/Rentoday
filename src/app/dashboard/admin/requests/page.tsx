import RequestsContent from './requests-content'

// Admin → Booking Requests management. Access is guarded by the admin layout.
export default function AdminRequestsPage() {
  return <RequestsContent />
}
